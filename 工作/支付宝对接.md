- 在支付宝端，partnerId 与 out_trade_no 唯一对应一笔单据，商家端保证不同次支付 out_trade_no 不可重复；若重复，支付宝会关联到原单据，基本信息一致的情况下会以原单据为准进行支付。
- redundant 
if test="forUpdate != null and forUpdate"> 里的== 


Spring中，在方法A中调用一个有@Transactional(rollbackFor = Exception.class)注解的方法B，若方法B中发生异常，那么，在方法A中能捕获到异常吗？


开始检查支付中的订单任务-超时关闭 添加调支付宝接口关闭订单

后端关闭订单后 用户付款了 

订单未支付，是否能重新支付


